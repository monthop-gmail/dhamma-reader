/**
 * วิธีรัน: 
 * 1. เปิดหน้าเว็บ Dek-D บทความที่คุณต้องการอ่านใน Chrome
 * 2. กด F12 เลือก Tab "Console"
 * 3. Copy Code ด้านล่างนี้ไปวางแล้วกด Enter
 * 4. รอจนระบบดึงครบ จะได้ไฟล์ .zip หรือข้อความให้ Copy ครับ
 */

(async () => {
    console.log("🚀 Starting Browser Console Scraper...");
    const playlist = [
        { order: 0, url: "https://writer.dek-d.com/dekdee/writer/viewlongc.php?id=2485901&chapter=0", title: "ข้อมูลเบื้องต้น" },
        // ... เพิ่มรายการตรงนี้ (หรือให้สคริปต์ดึงจากหน้าเว็บเอง)
    ];

    // ในที่นี้ สคริปต์จะดึงบทความที่กำลังเปิดอยู่บทเดียวเป็นตัวอย่าง
    // หรือคุณจะรันทีละบทในหน้าของบทนั้นๆ ก็ได้ครับ

    const content = document.querySelector('#story-content')?.innerHTML;
    const title = document.querySelector('.title-chapter')?.innerText;

    if (content) {
        const data = JSON.stringify({
            title: title || "บทความ",
            html: document.documentElement.outerHTML,
            scrapedAt: new Date().toISOString()
        }, null, 2);

        console.log("✅ ดึงข้อมูลสำเร็จ! Copy ข้อความด้านล่างนี้ไปเซฟเป็นไฟล์ .json ในเครื่องนะครับ:");
        console.log(data);
    } else {
        console.error("❌ ไม่พบเนื้อหาบทความ กรุณาเปิดหน้าบทความก่อนรันครับ");
    }
})();
